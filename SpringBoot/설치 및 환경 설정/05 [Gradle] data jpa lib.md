# Deprecated Gradle features were used in this build, making it incompatible with Gradle 9.0.
```
해결 방법

dependencies 에서 data jpa 를 add해서 프로젝트를 생성했는데,
기존 data가 없었고, 설정을 안해줘서 문제가 발생한거임

그래서 새 프로젝트를 생성해줬는데 data jpa를 제외하고 프로젝트를 생성해줌
그러니깐 됨
```
